{
  "name": "@quickjob/config",
  "version": "0.1.0",
  "private": true,
  "description": "Schémas de validation partagés (env, Money) — sans dépendance régionale.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "4.6.5"
  },
  "devDependencies": {
    "rimraf": "6.0.1",
    "typescript": "6.0.3"
  }
}
